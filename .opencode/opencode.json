{
  "$schema": "https://opencode.ai/config.json",

  "instructions": ["AGENTS.md"],

  "command": {
    "review-push": {
      "description": "审阅新增/变更的笔记，确认后推送到 Git 仓库",
      "template": "执行以下步骤，每完成一步向用户报告结果：\n\n1. 运行 `git status --porcelain` 检查工作区变更\n2. 如果没有变更，告知用户并结束\n3. 如果有变更，列出变更文件并审阅：\n   - 对新增的 .md 文件，检查是否有以下问题：\n     * 缺少一级标题\n     * 内容为空或过短（少于 100 字）\n   - 报告审阅结果\n4. 询问用户是否确认提交和推送\n5. 用户确认后，执行：\n   - `git add -A`\n   - `git commit -m \"<根据变更自动生成提交信息>\"`\n   - `git push`\n6. 报告推送结果"
    },
    "review": {
      "description": "审阅所有笔记文件，检查格式和质量问题",
      "template": "审阅整个仓库中的所有 .md 笔记文件，检查以下问题并生成报告：\n1. 缺少一级标题的文件\n2. 内容不足 200 字的文件\n3. 有明显拼写错误的文件\n4. 有死链/错误链接的错误\n\n列出所有问题文件及其具体问题。"
    }
  }
}