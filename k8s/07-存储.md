# Kubernetes 存储

## 存储体系

K8s 的存储体系分为三个层次：

```
StorageClass（存储模板 - 动态供应）
     ↓
PV（PersistentVolume - 集群存储资源）
     ↓
PVC（PersistentVolumeClaim - 用户存储请求）
     ↓
Pod（消费存储）
```

---

## Volume（卷）

Volume 是 Pod 中容器共享的存储目录，生命周期与 Pod 一致。

### emptyDir（临时存储）

Pod 创建时创建，Pod 删除时清除。用于容器间共享数据。

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: shared-volume-demo
spec:
  containers:
    - name: writer
      image: busybox
      command: ["sh", "-c", "echo data > /data/info.txt && sleep 3600"]
      volumeMounts:
        - name: shared-data
          mountPath: /data
    - name: reader
      image: busybox
      command: ["sh", "-c", "cat /data/info.txt && sleep 3600"]
      volumeMounts:
        - name: shared-data
          mountPath: /data
  volumes:
    - name: shared-data
      emptyDir:
        medium: Memory       # 使用内存（tmpfs），提升性能
        sizeLimit: 256Mi     # 大小限制
```

### hostPath（宿主机路径）

将宿主机目录挂载到 Pod（**生产慎用**）：

```yaml
volumes:
  - name: host-logs
    hostPath:
      path: /var/log        # 宿主机路径
      type: Directory        # 类型检查

# hostPath 类型
# Directory          - 目录必须存在
# DirectoryOrCreate  - 目录不存在则创建
# File               - 文件必须存在
# FileOrCreate       - 文件不存在则创建
# Socket             - UNIX Socket
```

---

## PV 和 PVC

### PV（PersistentVolume）

PV 是集群管理员预置的存储资源：

```yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-manual
spec:
  capacity:
    storage: 100Gi
  volumeMode: Filesystem          # Filesystem / Block
  accessModes:
    - ReadWriteOnce               # 单节点读写
    # - ReadOnlyMany              # 多节点只读
    # - ReadWriteMany             # 多节点读写
  persistentVolumeReclaimPolicy: Retain  # 回收策略
  storageClassName: manual
  nfs:
    server: 192.168.1.100
    path: /exported/path
```

### PVC（PersistentVolumeClaim）

PVC 是用户的存储申请：

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: manual
```

### PV 与 PVC 绑定

PVC 根据以下条件匹配 PV：
1. **存储大小**：PV 容量 >= PVC 请求
2. **访问模式**：PV 必须支持 PVC 的访问模式
3. **StorageClass**：必须匹配（或都为 ""）
4. **标签选择器**：可选

### PVC 使用

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: app-pvc
```

### 回收策略

| 策略 | 说明 |
|------|------|
| `Retain` | 保留 PV 及其数据，需要管理员手动回收 |
| `Delete` | 删除 PV 时同时删除底层存储 |
| `Recycle` | 已废弃，用动态供应替代 |

---

## StorageClass（动态存储供应）

StorageClass 允许按需自动创建 PV，**生产环境推荐使用**：

### 云厂商 StorageClass

```yaml
# AWS EBS
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: gp3
provisioner: ebs.csi.aws.com
parameters:
  type: gp3
  fsType: ext4
  encrypted: "true"
volumeBindingMode: WaitForFirstConsumer   # Pod 调度后再创建 PV

---
# GCE PD
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ssd
provisioner: pd.csi.storage.gke.io
parameters:
  type: pd-ssd
reclaimPolicy: Delete
allowVolumeExpansion: true

---
# Azure Disk
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: managed-premium
provisioner: disk.csi.azure.com
parameters:
  skuName: Premium_LRS
```

### 本地存储（Local PV）

```yaml
kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: local-storage
provisioner: kubernetes.io/no-provisioner  # 本地无法自动供应
volumeBindingMode: WaitForFirstConsumer
```

```yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: local-pv
spec:
  capacity:
    storage: 100Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /mnt/disks/ssd1
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - node-1
```

---

## NFS 存储

```yaml
# NFS StorageClass
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-csi
provisioner: nfs.csi.k8s.io
parameters:
  server: nfs-server.example.com
  share: /exports
reclaimPolicy: Delete
volumeBindingMode: Immediate
```

---

## CSI（Container Storage Interface）

CSI 是 K8s 存储的标准插件接口，所有主流存储都实现了 CSI：

```yaml
# 安装 CSI 驱动
kubectl apply -f https://raw.githubusercontent.com/kubernetes-csi/csi-driver-nfs/master/deploy/example/nfs-provisioner.yaml

# 使用 CSI 驱动
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-csi
```

---

## Ephemeral Volume（临时卷）

### Generic Ephemeral Volume

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: ephemeral-demo
spec:
  containers:
    - name: app
      image: nginx
      volumeMounts:
        - name: cache
          mountPath: /cache
  volumes:
    - name: cache
      ephemeral:
        volumeClaimTemplate:
          spec:
            accessModes: ["ReadWriteOnce"]
            storageClassName: gp3
            resources:
              requests:
                storage: 10Gi
```

---

## 常见存储拓扑

### 单节点读写（RWO）场景

```
Pod → PVC → PV → AWS EBS / GCE PD / Azure Disk
                → 只能挂载到一个节点
```

### 多节点读写（RWX）场景

```
Pod A ─┐
Pod B ─┼─> PVC → PV → NFS / GlusterFS / CephFS
Pod C ─┘       → 可挂载到多个节点
```

---

## Volume 挂载子路径

```yaml
containers:
  - name: app
    image: nginx
    volumeMounts:
      - name: data
        mountPath: /usr/share/nginx/html
        subPath: html        # 只挂载 Volume 中的 html 目录
        readOnly: true
      - name: config
        mountPath: /etc/nginx/nginx.conf
        subPath: nginx.conf  # 挂载单个文件
```

---

## 存储选型建议

| 存储方案 | RWO | RWX | 性能 | 适用场景 |
|----------|-----|-----|------|----------|
| AWS EBS | ✅ | ❌ | ⭐⭐⭐⭐ | 数据库、有状态应用 |
| GCE PD | ✅ | ❌ | ⭐⭐⭐⭐ | 数据库、有状态应用 |
| Azure Disk | ✅ | ❌ | ⭐⭐⭐⭐ | 数据库、有状态应用 |
| AWS EFS | ✅ | ✅ | ⭐⭐ | 共享存储、静态文件 |
| NFS | ✅ | ✅ | ⭐⭐ | 企业内部共享存储 |
| Ceph RBD | ✅ | ✅ | ⭐⭐⭐ | 高性能共享存储 |
| Local SSD | ✅ | ❌ | ⭐⭐⭐⭐⭐ | 高吞吐本地缓存 |
| MinIO | ✅ | ❌ | ⭐⭐⭐ | 对象存储 |

## 生产实践建议

1. **数据库用 StatefulSet + PVC**，不要用 Deployment
2. **PVC 名称不要重复**，删除 StatefulSet 不会自动删除 PVC
3. **StorageClass 设置 `WaitForFirstConsumer`**，延迟绑定到 Pod 调度的节点
4. **开启 `allowVolumeExpansion`**，支持在线扩容
5. **PV 回收策略用 `Delete`**（开发环境）或 `Retain`（生产环境）
6. **共享存储选 CSI + NFS** 或云厂商的共享文件系统
7. **监控 PVC 使用率**，设置告警避免磁盘写满
8. **Local PV 需要配合节点故障转移机制**（如 OpenEBS）

## 官方文档

- 存储概述：https://kubernetes.io/docs/concepts/storage/
- PV/PVC：https://kubernetes.io/docs/concepts/storage/persistent-volumes/
- StorageClass：https://kubernetes.io/docs/concepts/storage/storage-classes/
- CSI：https://kubernetes.io/docs/concepts/storage/volumes/#csi
